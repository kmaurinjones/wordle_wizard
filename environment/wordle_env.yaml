# Install the conda environment by running 
# the following command in your conda base environment:
# conda env create -f hearts_scores.yaml
name: 'wordle'
channels:
  - conda-forge
  - defaults
dependencies:
  - urllib
  - operator
  - random
